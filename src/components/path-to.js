import React from "react";

const Pathto = () => {
    return (
        <div>
            <img src={require('../img/logo.png')} alt={require('../img/logo.png')}/>
        </div>
    );
  };

  export default Pathto;
